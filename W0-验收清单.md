# W0 验收清单 — 环境与骨架准备

> 生成时间：2026-09-15 ｜ 对应《项目实施计划方案》W0 章节 ｜ 基线提交：`6d16d76`

## 一、交付物清单

| 类别 | 位置 | 内容 |
|------|------|------|
| 后端工程 | `backend/` | 父 POM + 9 模块（common、gateway、user-credit / trade / lostfound / errand / ai-assistant / notify / admin-service） |
| 公共组件 | `backend/common` | Result 统一响应、异常体系（19 条错误码）、Outbox 模板、幂等注切面、MDC 日志过滤器、21 个业务枚举 |
| 容器编排 | `docker/` | docker-compose.yml（常驻 5 容器 + app profile）、观测组合 compose、Nginx 配置、Prometheus/Loki 配置 |
| 数据库 | `docker/sql/` | 7 库初始化、27 张表 / 69 索引 DDL、7 账号建权、审计表防删保护、种子数据 |
| 备份 | `docker/backup/` | 每日 mysqldump 备份脚本（保留 30 天） |
| 一键脚本 | `scripts/` | dev-up.bat / dev-up-obs.bat / dev-down.bat / dev-status.bat |
| 生成器 | `tools/` | gen_backend / gen_common / gen_sql / gen_docker（可重跑） |
| 架构文档 | `docs/` | 10 份架构文档 + 安全设计配图 |

## 二、自动校验结果（已通过）

| # | 校验项 | 结果 |
|---|--------|------|
| 1 | Maven 编译 | ✅ 10 模块 Reactor 全部 SUCCESS（2026-09-15 复核，31s，JDK 21.0.11） |
| 2 | compose 语法 | ✅ `docker compose config` 通过，常驻 5 + 观测 4 容器 |
| 3 | DDL 静态校验 | ✅ 27 表 / 69 索引，库归属与设计一致 |
| 4 | 内存预算（静态） | ✅ 常驻容器 mem_limit 合计 2.88GB ≤ 3GB |
| 5 | 密钥红线 | ✅ `docker/.env` 未入库（随机强口令），`.env.example` 模板正常入库，首次提交 106 文件无敏感泄漏 |
| 6 | 安全基线 | ✅ SB-01~SB-05 落地（Nacos 鉴权、Sentinel 口令、Actuator 仅 health+info、server_tokens off、CORS 白名单） |

## 三、待用户手动执行（唯一剩余项）

沙箱限制导致 AI 无法启动 Docker Desktop（依赖 WSL2），请手动完成：

1. 启动 Docker Desktop，等待引擎就绪；
2. 双击 `scripts\dev-up.bat` —— 一键拉起 mysql / redis / nacos / sentinel / nginx 五个常驻容器；
3. 双击 `scripts\dev-status.bat` —— 导出内存基线，对照 2.88GB 预算（此项即 W0 退出标准的最后一项）；
4. 可选：`scripts\dev-up-obs.bat` 拉起 Prometheus / Grafana / Zipkin / Loki 观测组合。

> 停止环境：`scripts\dev-down.bat`。容器数据落在 `docker/data/`（已 gitignore）。

## 四、待裁决口径（W1 前确认）

| # | 事项 | 现状 | 待裁决 |
|---|------|------|--------|
| 1 | 表数量口径 | 实施计划写「7 库 15 表」，实际落地 27 表（9 核心域 + 15 总览全量 + 归档表） | 以 27 表为准，回改计划文档？ |
| 2 | 成功响应码 | 文档未定义，W0 暂定 `"000000"` | 确认或另定 |
| 3 | common 类数量 | 现 38 个类，§2.2.2 C-05 约束「≤10 个类」 | 放宽约束或拆分模块 |
| 4 | SCA 版本矩阵 | Boot 3.4.5 + Spring Cloud Alibaba 2023.0.3.2 兼容性待实跑验证 | W1 首日验证，备选降 Boot 3.3.x |

## 五、W1 预告

接入 Spring Cloud Alibaba（Nacos 注册/配置中心 + Sentinel 限流）并实现用户信用服务的核心接口；POM 中 SCA / Dubbo / Spring AI 依赖已以注释形式预置，届时启用。
