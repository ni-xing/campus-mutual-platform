# AGENTS.md

## Agent skills

### Issue tracker

Issue 与 spec 存在本仓库的 GitHub Issues（`ni-xing/campus-mutual-platform`），统一用 `gh` CLI 操作。见 `docs/agents/issue-tracker.md`。

### Triage labels

五类 triage 角色在 tracker 中使用同名标签（`needs-triage` / `needs-info` / `ready-for-agent` / `ready-for-human` / `wontfix`）。见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文（single-context）仓库：仓库根目录一份 `CONTEXT.md` + `docs/adr/`。见 `docs/agents/domain.md`。
