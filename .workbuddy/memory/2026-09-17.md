# 2026-09-17

## Spec coding 推进（用户裁决记录）
- 用户确认三裁决：成功码维持 000000；common 类约束放宽（按职责分包）；W1 范围=spec+实现一起（1.1 网关JWT+1.2 注册登录+1.3 隐私同意+1.4 信用表+1.6 MDC）。
- 发现 docs/specs/ 目录（00 全局约定 + 01 二手 + 02 失物 + 03 跑腿 + 04 AI），按模块组织的 spec 体系，缺用户认证 spec。
- 用户新裁决：下一步=补全所需 spec 文档。
- 产出：docs/specs/05-用户与认证.md（U-01~09，AC-1~12，物理表对齐 user_db 三表，含隐私同意两列新增约定、评星→信用分映射 1:-5/2:-2/3:+1/4:+3/5:+5、违约-10）。
- 回填漂移（以已落地 DDL/Result 为基准）：00 响应体 {code,msg,data,traceId,timestamp}+成功码 000000；01/02/03 §5 逻辑表名映射物理表（t_trade_order、t_balance_flow 承载支付流水、t_claim_apply、t_notice、credit_score 统一口径）；积分列 W2 落库时定。
- 状态：spec 层完成待用户审；实现顺序：05 认证 → 01 二手 → 03 跑腿 → 02 失物 → 04 AI。W0 遗留 SCA 版本矩阵验证（Boot 3.4.5+SCA 2023.0.3.2，备选降 Boot 3.3.x）在 05 实现首日做。

## 05 认证模块实现（feat W1，commit 08ba899，54 文件 +2140 行）
- spec 裁决追加：身份源=MVP 校园邮箱白名单，不做教务对接；QQ 登录需备案域名放 W7+，微信网站扫码需企业资质明确不做；预留 IdentityProvider 适配器 + t_user_identity 绑定表（uk_idp_openid）。
- 代码：common（JwtSupport/UserContextInterceptor/TraceMdcWebFilter/BizException extraMsg）；user-credit（注册 BCrypt cost10+白名单+隐私留痕、登录失败5次锁15min+防枚举 DUMMY_HASH+互踢黑名单 TTL=剩余有效期、登出、脱敏、credit adjust 幂等）；gateway（JwtAuthGlobalFilter /internal/** 404+黑名单+透传头防伪造、RedisRateLimiter 500/60）。
- 版本矩阵：Boot 3.4.5 + SC 2024.0.0 + SCA 2023.0.3.2 编译通过；SC 2024.0.0 用旧 artifactId spring-cloud-starter-gateway（gateway-server-webflux 是 2025.x 的）。
- 发现并修复 gen_docker.py 未同步 W0 nginx 动态 upstream 的回归（重跑生成器会回退 campus.conf，已在源头模板恢复 resolver+变量式 proxy_pass）。
- 落库变更：t_user_account + privacy_consent_version/privacy_consent_time 两列；新表 t_user_identity。user_db 现有容器数据需重建（dev 掉卷）或 ALTER。
- 待办（任务#4）：容器联调跑通 AC-1~12、单测、Nacos 运行期兼容验证、user_db 重建。IDE 直跑需设 JWT_SECRET/NACOS_USER/NACOS_PASSWORD env。

## 最小可演示闭环达成（commit d7c01fe）
- 前端：frontend/ Vue3+Vite+Pinia 三页（LoginView 包装 ?mode=register 切注册，PrivacyView 静态 PIPL 文案，HomeView 学号脱敏+信用分）；api.js 统一封装 401 清 session；路由守卫。npm 用 managed node22 + 项目内 .npm-cache（系统 node24 cache 权限 EPERM）。
- 全链路验证：浏览器 5173 -> vite proxy -> nginx 8088 -> gateway 8080 -> user-credit -> MySQL/Redis，登录返回 token 290 位。
- 联调修复 6 处：common 自动装配 Servlet/WebFlux 类隔离、OutboxTemplate 移出通用装配、IdempotentAspect request 改方法参数注入、KeyResolver @Primary、/internal 路由+NoHandlerFound 404、注册 token 补 active 映射。
- 基建修复：gen_docker.py 幂等化（重跑不再轮换 .env，根因是此前所有密码不匹配的源头）、mysql 服务补 DB_PWD_* env（W0 漏检）、scripts/init-nacos-admin.py 初始化 Nacos 密码。
- 验收：scripts/api_test_w1.py 12/12 全绿（注册/重复/白名单邮箱/弱密码/隐私勾选/401/internal 404/脱敏/防枚举/互踢/登出）。
- 常用命令：docker compose up -d（5常驻）+ --profile app up -d gateway user-credit-service；npm run dev（frontend/）。IDE 直跑需 JWT_SECRET/NACOS_USER/NACOS_PASSWORD。

## 管理员账号修复（commit 14e11bc）
- 有 seed 管理员：ADMIN0001 / Admin@Campus2026（.env 的 SEED_ADMIN_PASSWORD），但此前从未生效。
- 根因链：17-audit-protection.sh 对库级授权的账号做表级 REVOKE DELETE -> ERROR 1147 -> set -e 中断 init -> 20-seed-data.sql 从未执行；且 seed 里 ADMIN hash 是占位假值、余额账户写死 user_id=1。
- 修复：17 改为整库 REVOKE ALL + 重授 SELECT/INSERT/UPDATE（审计库无 DELETE）；20 换真实 BCrypt(b, Spring Security 兼容) + user_id 子查询动态绑定；gen_sql.py 源头同步。
- 运行库已手工补灌（容器内挂载文件 + --default-character-set=utf8mb4；注意 python->docker exec 直接传中文 SQL 会 latin1 双重编码，必须走容器内文件）。
- 验证：API 登录 000000，/users/me 返回 平台管理员/ADMI****01/credit 100。
- 管理端 API（admin_db 的后台接口）尚未实现，属后续周次范围。

## 前端界面按 docs/design 设计方案改造（搭把手 · 布告栏风格）
- 参考：`docs/design/设计方案.md` + `index.html`（Web 落地页）+ `mobile.html`（移动端三屏原型），视觉主张"宿舍楼下的布告栏"（方格稿纸底 / 便签 / 图钉 / 墨水蓝 / 便利贴黄 / 图钉红急章 / 霞鹜文楷）。
- 改动（frontend/）：`index.html` 标题改"搭把手"+文楷 CDN；新增 `src/styles/main.css` 全局设计 token（--paper/--grid/--ink/--highlight/--pin/--board）+ 共享组件类（.card-note 便签卡带图钉、.post 布告卡带急章、.btn/.btn.yellow/.btn-ghost、.field、pin-in 钉入动画、prefers-reduced-motion 关停）；`main.js` 引入；App.vue 顶栏"搭把手。"手写 logo；Login/Register 便签卡风格（Register 主按钮黄色 CTA）；Privacy 加"模拟"红章+稿纸段落；HomeView 改"楼下布告流"（时段问候、搜索过滤、四快捷入口、演示布告流：失物黄底急章/价格图钉红、信用分文楷三档色）。逻辑（api/auth/router）未动。
- 验证：npm install + build 通过（42 模块）；dev server 5174（5173 被旧进程占用）。布告流为静态演示数据，W2+ 接真实接口替换。

## 新增落地页（用户反馈：直接进登录页太空旷，网站应有主页）
- 新增 `LandingView.vue` 挂 `/`：完整移植 docs/design/index.html——Hero（手写大标题+黄色高亮+木框黑板绿布告栏 4 便签钉入动画）、炭笔黑今日布告滚动条、四板块非对称网格（黄/米/蓝/黑板绿反白，急章）、三步流程、黑板绿 CTA 区、页脚。已登录则 CTA 变"进入布告栏"。
- 布告流页挪到 `/board`（requireAuth），登录/注册成功后 replace('/board')，退出登录回 `/`，守卫同步。
- App.vue 顶栏改全宽站点导航（max-width 1080px）：logo + 落地页锚点导航（仅落地页显示）+ 模拟环境标签 + 登录/进入布告栏按钮；新增全局 `.container-narrow`（520px）给内页用。
- 构建通过（44 模块，LandingView 6.83kB css）。
